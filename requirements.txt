# ╔══════════════════════════════════════════════════════════════╗
# ║   BOXING AI — Python Requirements                           ║
# ║   Install: pip install -r requirements.txt                  ║
# ║                                                             ║
# ║   IMPORTANT — PyTorch must be installed FIRST               ║
# ║   with the correct CUDA version for your GPU.               ║
# ║                                                             ║
# ║   For GTX 1650 (CUDA 11.8):                                 ║
# ║   pip install torch torchvision --index-url                 ║
# ║     https://download.pytorch.org/whl/cu118                  ║
# ║                                                             ║
# ║   CPU only (no GPU):                                        ║
# ║   pip install torch torchvision                             ║
# ║                                                             ║
# ║   After PyTorch is installed, run:                          ║
# ║   pip install -r requirements.txt                           ║
# ╚══════════════════════════════════════════════════════════════╝


# ── Core ML ───────────────────────────────────────────────────
# torch and torchvision installed separately (see above)
# Listed here so pip freeze includes them
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0

# ── Computer Vision & Pose Estimation ────────────────────────
mediapipe>=0.10.0
opencv-python>=4.8.0

# ── Signal Processing (extract_from_videos.py peak detection) ─
scipy>=1.11.0

# ── Training Evaluation & Plots ──────────────────────────────
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Web Server (server.py) ────────────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
pydantic>=2.0.0

# ── Image Processing (server.py base64 frame encoding) ───────
Pillow>=10.0.0

# ── Utilities ─────────────────────────────────────────────────
pathlib2>=2.3.7     # Path handling (Python 3.4+ has pathlib built-in, this is a fallback)